# Em produção no Netlify (mesmo site), deixe vazio ou use /api
# O frontend já faz fallback para /api quando VITE_API_URL não está definido.
# VITE_API_URL=/api

# Só use URL absoluta se a API estiver em outro domínio:
# VITE_API_URL=https://seu-app.netlify.app/api
